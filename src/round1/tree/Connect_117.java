package round1.tree;

import java.util.Queue;

public class Connect_117 {
    public Node connect(Node root) {
        if (root == null)
            return root;
        // 辅助队列
        return root;
    }
}
